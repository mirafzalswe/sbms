[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = ">=2.31.0"
urllib3 = ">=2.1.0"
python-dotenv = ">=1.0.0"
flask = ">=3.0.0"
openpyxl = ">=3.0.0"
pdfplumber = ">=0.11.0"
python-docx = ">=1.1.0"
gunicorn = ">=21.2.0"
waitress = ">=3.0.0"

[dev-packages]

[requires]
python_version = "3.14"
